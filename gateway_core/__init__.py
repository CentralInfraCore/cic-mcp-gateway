"""gateway_core — cic-mcp-gateway's first gateway-specific Python package.

Job: session-context-pack-v1-001. Before this job, this repo contained no
gateway-specific Python module — only the inherited base-repo MCP/KB
template (mcp-server/server.py, make_source.py). This package holds the
gateway's own compile-time logic (currently: compile_context, the
cic-mcp-session adapter implementation described by
output/gateway-session-adapter-contract.md).
"""
